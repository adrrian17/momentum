<div class="flex flex-col mt-6 bg-slate-100 dark:bg-gray-800 px-8 md:px-0">
  <div class="max-w-2xl mx-auto py-10">
    <div class="flex flex-col items-center">
      <h3 class="font-work-sans text-2xl font-bold mb-2 text-center text-slate-800 dark:text-gray-200">Tal vez te
        pueda gustar...</h3>
      <div class="border-2 w-1/4 rounded-full border-brand-400 mt-1"></div>
    </div>
    <div class="flex flex-col md:flex-row justify-center space-y-8 md:space-y-0 md:space-x-8 mt-8">
      {{#get "posts" limit="3" filter="id:-{{id}}+tag:-[comic,guiones]" as |related|}}
        {{#foreach related}}
          {{> related-card}}
        {{/foreach}}
      {{/get}}
    </div>
  </div>
</div>