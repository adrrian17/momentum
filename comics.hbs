{{!< default}}
{{#page}}
    <div class="flex flex-col max-w-2xl mx-auto mt-14">
        <div class="flex flex-col items-center mb-12">
            <div class="mb-6">
                <h1 class="font-work-sans text-3xl font-bold text-slate-800">{{title}}</h1>
            </div>
            <p class="font-work-sans text-lg text-slate-800">
                Todos los cómics que he escrito y publicado.
            </p>
        </div>
        <div class="post-content">
            {{content}}
        </div>
    </div>
{{/page}}